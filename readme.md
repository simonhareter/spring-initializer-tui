Idea: 
Make a spring-initializer-tui instead of using the web version or the spring boot cli.
Same behaviour as spring initializer but it also gets automatically unzipped and ready to use.

Use the spring api to retrieve the metadata exposed by spring initializer.

